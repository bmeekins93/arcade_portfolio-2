# Systems
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
__MACOSX/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.*.local


# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
.idea/
*.swp
*.swo

# Build Artifacts
# We generally want to track dists for the portfolio unless they are generated by CI
# But since this is a mono-repo style, we might want to exclude source repo builds if we aren't using them
# For now, excluding top level builds if any
dist/
build/
